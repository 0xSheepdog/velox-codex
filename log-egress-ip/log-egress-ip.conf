# log-egress-ip.conf
# logrotate.d config file
# v0.1, by 0xSheepdog
# useage:
# - used with log-egress-ip.sh (ususally in /etc/cron.daily)
/var/log/egress-ip.log {
  size 1M
  monthly
  rotate 12
  compress
  dateext
  missingok
}
